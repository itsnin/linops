#!/usr/bin/env bash
set -euo pipefail
# scaffold only real commands not written yet
# will apt-mark hold the verified package list not a pin file
# see debloat.sh section 11 for the exact package list
