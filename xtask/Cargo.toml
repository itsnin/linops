[package]
name = "xtask"
description = "project local dev tooling run via cargo run --package xtask not part of the shipped binary"
edition.workspace = true
version.workspace = true
license.workspace = true
publish = false

[dependencies]
